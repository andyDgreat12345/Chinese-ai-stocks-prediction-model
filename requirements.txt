# Core scheduling
APScheduler>=3.10

# Data ingestion (Phase 2 — not yet wired)
yfinance>=0.2.40      # US indices, sector ETFs, precious metals
akshare>=1.13         # China A-share indices / sectors / macro
feedparser>=6.0       # World-news RSS (Reuters/Caixin/Bloomberg)

# Analysis
numpy>=1.26
pandas>=2.1

# Local API + dashboard (Phase 4/5)
fastapi>=0.110
uvicorn>=0.29

# Optional LLM reflection backend (spec §4b-iii). Only needed if
# ORACLE_LLM_PROVIDER=claude; DeepSeek uses stdlib. Imported lazily, so the
# core system and test suite run without it.
anthropic>=0.40

# Dev / test
pytest>=8.0
httpx>=0.27          # FastAPI TestClient (API smoke tests)
