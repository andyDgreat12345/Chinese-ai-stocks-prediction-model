"""Research subsystem — the wide correlation sweep and its report.

`universe` defines a hypothesis-driven US symbol set; `sweep` runs every
US↔China pair across multiple lags with significance testing, multiple-comparison
control, and out-of-sample stability checks; `report` renders the findings.
"""
